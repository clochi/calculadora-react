import React from 'react';

const Controles = props => (
  <form>
    <input type="text" placeholder="Monto"/>
    <input type="text" placeholder="TNA"/>
    <button>Calcular</button>
  </form>
)

export default Controles;
